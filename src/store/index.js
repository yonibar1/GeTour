import Vue from 'vue'
import Vuex from 'vuex'
import { tourStore } from './tourStore.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tourStore
  }
})
